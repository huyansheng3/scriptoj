/**
 * https://scriptoj.com/problems/101
 * extractStr('My name is:Jerry. My age is:12.') // => ['Jerry', '12']
 */

const extractStr = str => {
  d
  const list = []
  while ((myArr = re.exec(str)) !== null) {
    list.push(myArr[1])
  }
  return list
}
